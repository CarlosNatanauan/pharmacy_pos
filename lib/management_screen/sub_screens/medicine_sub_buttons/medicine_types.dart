import 'package:flutter/material.dart';

class MedicineTypes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Medicine Types Screen',
        style: TextStyle(fontSize: 24, color: Colors.white),
      ),
    );
  }
}
